// Package usecase implements order-service's application logic: validating an
// incoming order against its trading pair and handing it off for matching via
// the configured OrderProducer.
package usecase
